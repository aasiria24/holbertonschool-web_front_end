# holbertonschool-web_front_end

## 📁 `html_advanced`

Contains the **pure HTML** version of the Techium project.  
It includes 40+ HTML files that progressively build a complete homepage and a detailed styleguide.  
The directory demonstrates semantic markup, accessibility features, and embedding of media (video, audio, iframes) without any CSS.
